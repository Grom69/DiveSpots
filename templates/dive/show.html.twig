{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
	<div class="bg-primary">
		<div class="container w-50 bg-light mt-5 rounded p-0 mb-5">
			<div class="d-flex justify-content-center"><img src="{{ dive.picture }}" id="details-photo" class="card-img-top rounded" alt="dive"></div>
			<div class="">
				<div class="bg-primary">
					<h5 class="card-title text-center pt-3 mb-2 text-white">{{dive.city}}
					</h5>
					<p class="card-text text-center text-white pb-3">{{dive.country}}</p>
				</div>
				<div class="w-100">
					<iframe src="https://maps.google.com/maps?&q={{dive.country}}+{{dive.city}}&output=embed" height="220" class="w-100" style="border:0;"></iframe>
				</div>
				<p class="card-text p-4">{{dive.description}}</p>
				<a href="#" class="btn {%  if is_granted('ROLE_ADMIN') %} btn-info {% else %} btn-light {% endif %}  text-center w-100">Détails</a>
			</div>
		</div>
	</div>
{% endblock %}
{% block javascripts %}
	{{ parent() }}
{% endblock %}
